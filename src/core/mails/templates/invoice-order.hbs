<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Factura de orden #{{ invoiceOrderDto.order.id }} - Tienda NJ</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      max-width: 600px;
      margin: auto;
      padding: 20px;
      color: #333;
    }
    h1, h2 {
      text-align: center;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 20px;
    }
    th, td {
      padding: 8px;
      border: 1px solid #ccc;
      text-align: left;
    }
    .total {
      text-align: right;
      font-size: 1.2em;
    }
  </style>
</head>
<body>

  <h1>Factura de orden #{{ invoiceOrderDto.order.id }}</h1>
  <p><strong>Fecha:</strong> {{ invoiceOrderDto.order.createdAt }}</p>

  <h2>Datos del Cliente</h2>
  {{#if invoiceOrderDto.user.people}}
    <p><strong>Nombre:</strong> {{ invoiceOrderDto.user.people.name }} {{ invoiceOrderDto.user.people.lastname }}</p>
    <p><strong>DNI:</strong> {{ invoiceOrderDto.user.people.dni }}</p>
    <p><strong>Email:</strong> {{ invoiceOrderDto.user.people.email }}</p>
    <p><strong>Teléfono:</strong> {{ invoiceOrderDto.user.people.phone }}</p>
  {{else}}
    <p>Datos del cliente no disponibles</p>
  {{/if}}

  <h2>Productos</h2>
  {{#if invoiceOrderDto.order.productOrder}}
    <table>
      <thead>
        <tr>
          <th>Producto</th>
          <th>Cantidad</th>
          <th>Precio unitario</th>
          <th>Subtotal</th>
        </tr>
      </thead>
      <tbody>
        {{#each invoiceOrderDto.order.productOrder}}
          <tr>
            <td>{{#if this.product.product}}{{ this.product.product.product }}{{else}}Producto no disponible{{/if}}</td>
            <td>{{ this.amount }}</td>
            <td>${{#if this.product.price}}{{ this.product.price }}{{else}}0{{/if}}</td>
            <td>${{#if this.subtotal}}{{ this.subtotal }}{{else}}0{{/if}}</td>
          </tr>
        {{/each}}
      </tbody>
    </table>
  {{else}}
    <p>No hay productos en esta orden</p>
  {{/if}}

  <p class="total"><strong>Total:</strong> ${{#if invoiceOrderDto.order.total}}{{ invoiceOrderDto.order.total }}{{else}}0{{/if}}</p>

  <h2>Resumen</h2>
  {{#if invoiceOrderDto.order.payment}}
    <p><strong>Método de pago:</strong> {{#if invoiceOrderDto.order.payment.method}}{{ invoiceOrderDto.order.payment.method.method }}{{else}}No especificado{{/if}}</p>
    <p><strong>Estado de pago:</strong> {{#if invoiceOrderDto.order.payment.status}}{{ invoiceOrderDto.order.payment.status.status }}{{else}}No especificado{{/if}}</p>
  {{else}}
    <p><strong>Método de pago:</strong> No especificado</p>
    <p><strong>Estado de pago:</strong> No especificado</p>
  {{/if}}
  
  {{#if invoiceOrderDto.order.shipment}}
    <p><strong>Estado de envío:</strong> {{#if invoiceOrderDto.order.shipment.status}}{{ invoiceOrderDto.order.shipment.status.status }}{{else}}No especificado{{/if}}</p>
  {{else}}
    <p><strong>Estado de envío:</strong> No especificado</p>
  {{/if}}
  
  <p><strong>Tipo de orden:</strong> {{#if invoiceOrderDto.order.typeOrder}}{{ invoiceOrderDto.order.typeOrder.type }}{{else}}No especificado{{/if}}</p>

</body>
</html>
